#include <iostream>
#include "Empirical/include/emp/math/Random.hpp"

class Organism {
    public: 
        double behavior;
        
        Organism (double b) : behavior(b) {};
};

int main() {
    emp:: vector<Orgamism> population;

    for (int i = o, i < 100, i++) {
        std :: cout << "Behavior of Organism 0:" << population[0].behavior << std::endl;
    }

    
}